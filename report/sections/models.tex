\section{Архитектуры моделей}

Все модели построены по одной схеме: предобученный трансформерный энкодер принимает последовательность токенов и возвращает контекстуализированные представления размерности $H$; поверх него ставится классификационная голова, которая предсказывает BIO-метку для каждого токена. Различия между моделями касаются либо выбора энкодера, либо архитектуры головы, либо того и другого.

\subsection{Энкодеры}

Все энкодеры --- трансформеры с 12 слоями и $H = 768$, обученные самостоятельно на разных данных. Это позволяет сравнивать влияние доменного предобучения при фиксированной архитектуре.

\textbf{BERT}~\cite{devlin2019bert} (\texttt{bert-base-cased}) предобучен на корпусе общей тематики (BooksCorpus + Wikipedia). Он задаёт базовую линию: насколько хорошо работает общедоменная модель на специализированной задаче.

\textbf{RoBERTa}~\cite{liu2019roberta} (\texttt{roberta-base}) отличается от BERT более интенсивным обучением: больший корпус, динамическое маскирование и отказ от задачи предсказания следующего предложения (NSP). Ожидается, что эти изменения улучшат общее качество представлений, хотя отказ от NSP потенциально снижает чувствительность к межпредложенческим зависимостям.

\textbf{SciBERT}~\cite{beltagy2019scibert} (\texttt{allenai/scibert\_scivocab\_cased}) обучен на 1.14 млн научных статей из Semantic Scholar и имеет собственный научный словарь. Научные тексты содержат специфическую терминологию (названия методов, датасетов, метрик), которой нет в общедоменных корпусах, поэтому SciBERT должен давать существенное преимущество на SciERC.

\subsection{Классификационные головы}

Выбор головы определяет, насколько сложную функцию можно построить поверх представлений энкодера и учитываются ли зависимости между соседними метками.

\textbf{Linear.} Дропаут с вероятностью $p$ (берётся из конфига энкодера), затем линейный слой $\mathbb{R}^{H} \to \mathbb{R}^{K}$, где $K = 13$. Это минимальное решение: всю работу по различению классов делает энкодер, голова только проецирует. Обучается с токен-уровневой кросс-энтропией.

\textbf{MLP.} Дропаут $\to$ Linear($H$, 256) $\to$ GELU $\to$ Linear(256, $K$). По сравнению с Linear добавляет нелинейный слой, который может улучшить разделимость классов в пространстве признаков. Гипотеза: если представления энкодера не вполне линейно разделимы, MLP компенсирует это.

\textbf{Linear + CRF.} Линейный слой формирует эмиссионные потенциалы, поверх которых CRF-слой~\cite{lafferty2001crf} моделирует зависимости между соседними метками. Главное преимущество CRF --- принудительная BIO-согласованность: декодирование по алгоритму Витерби никогда не выдаст \texttt{I-X} без предшествующего \texttt{B-X}. Обучение ведётся по отрицательному логарифмическому правдоподобию последовательности, что сложнее токен-уровневой кросс-энтропии.

\textbf{Concat-4.} Вместо финального скрытого состояния берётся конкатенация выходов последних 4 слоёв энкодера: $\mathbb{R}^{4H}$, затем дропаут и линейный слой. Мотивация: нижние слои BERT захватывают морфологию и синтаксис, верхние --- семантику; их совместное использование может быть полезно для задачи, требующей и синтаксических (граница сущности), и семантических (тип сущности) сигналов.

\subsection{Взвешивание классов}

Метка \texttt{O} составляет около 70\% всех токенов. При равновесной кросс-энтропии модель может получать низкий лосс, по умолчанию предсказывая \texttt{O}, при этом плохо распознавая редкие типы. Чтобы компенсировать дисбаланс, применяются веса классов:
\[
    w_c = \sqrt{\frac{N}{K \cdot n_c}},
\]
где $N$ --- общее число токенов в обучающей выборке, $n_c$ --- число токенов класса $c$. Квадратный корень смягчает взвешивание: без него соотношение весов \texttt{O} и Metric достигало бы $35\times$, что чрезмерно штрафует частые классы и снижает точность. Sqrt снижает это соотношение примерно до $6\times$.

Каждая модель обучается в двух вариантах --- с весами и без, --- чтобы эмпирически проверить, помогает ли взвешивание на данном датасете.

\subsection{Полный список экспериментов}

\begin{table}[h]
\centering
\caption{Модели, участвующие в эксперименте}
\label{tab:models}
\begin{tabular}{llll}
\toprule
\textbf{Название} & \textbf{Энкодер} & \textbf{Голова} & \textbf{Веса} \\
\midrule
bert\_linear\_noweight        & BERT    & Linear     & --- \\
bert\_linear                  & BERT    & Linear     & \checkmark \\
roberta\_linear\_noweight     & RoBERTa & Linear     & --- \\
roberta\_linear               & RoBERTa & Linear     & \checkmark \\
scibert\_linear\_noweight     & SciBERT & Linear     & --- \\
scibert\_linear               & SciBERT & Linear     & \checkmark \\
scibert\_mlp                  & SciBERT & MLP        & --- \\
scibert\_linear\_crf\_noweight& SciBERT & Linear+CRF & --- \\
scibert\_linear\_crf          & SciBERT & Linear+CRF & \checkmark \\
scibert\_concat4              & SciBERT & Concat-4   & --- \\
\bottomrule
\end{tabular}
\end{table}
